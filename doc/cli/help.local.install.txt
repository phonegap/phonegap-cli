Usage: $0 local install <platform>

Description:

  Installs the application to a specific platform.

  When there is no application to install, the command will fail.

Examples:

  $ $0 local install android
