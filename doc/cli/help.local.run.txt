Usage: $0 local run <platform>

Description:

  Builds and installs your application for a specific platform.

  See the 'local build' and 'local install' commands for available [options].

Examples:

  $ $0 local run android
  $ $0 local run android --device
  $ $0 local run android --emulator
