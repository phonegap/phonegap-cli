# appveyor file
# http://www.appveyor.com/docs/appveyor-yml

environment:
  matrix:
  - nodejs_version: "10"
install:
  - npm install

build: off

test_script:
  - node --version
  - npm --version
  - npm test
